import { AppState } from "./types";
describe("app.selectors", () => {
  it(".....description", () => expect(true).toBeFalsy());
  /* 
    lag tester for å hente meny (getMenu) order (getOrder) fra AppState 
    skriv ulike tester for å håndtere grenseverdier som undefined, tomt objekt eller faktisk/forventet verdi
*/
});
